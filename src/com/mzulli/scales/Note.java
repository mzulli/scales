package com.mzulli.scales;

public class Note {
  int noteRow = 0;
  int noteCol = 0;

  public Note(int row, int col) {
    noteRow = row;
    noteCol = col;  
  }
  
  public getNote() {
    return Note(noteRow, noteCol);
  }  
}